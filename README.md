# Super lesson

## Getting Started

```sh
cp .env.example .env
pnpm install
pnpm dev
```

This starts your app in development mode, rebuilding assets on file changes.

Use `test@gmail.com` with `test` password to login

## Build

To build the app for production:

```sh
pnpm build
```
